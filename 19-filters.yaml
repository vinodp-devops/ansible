- name: Demo on Filters
  hosts: local
  connection: local
  vars:
    topics: linux, shell, ansible, terraform
  tasks: 
  - name: Greetings
    ansible.builtin.debug:
      msg: "Hi, Vinod {{ greets | default('How Are You?') }}"

  - name: Convert String to List
    ansible.builtin.debug:
      msg: "{{ topics | split(',').strip('') }}"